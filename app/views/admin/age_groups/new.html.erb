<i class="icon-backward"></i> <%= link_to 'Back', [:admin, @age_group] %>
<%= simple_form_for([:admin, @age_group], html: {class: 'form-holizonal'}) do |f| %>
  <%= render 'form', f: f %>
  <%= button_tag(type: 'submit', class: 'btn btn-primary') do %>
    <i class="icon-ok-circle"></i> <%= t 'helpers.submit.submit' %>
  <% end %>
<% end %>

