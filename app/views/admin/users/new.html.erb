<i class="icon-backward"></i> <%= link_to 'Back', :admin_users %>
<%= simple_form_for([:admin, @user], html: {class: 'form-holizonal'}) do |f| %>
  <%= render 'form', f: f %>
  <%= button_tag(type: 'submit', class: 'btn btn-primary') do %>
    <i class="icon-ok-circle"></i> <%= t 'helpers.submit.submit' %>
  <% end %>
<% end %>
